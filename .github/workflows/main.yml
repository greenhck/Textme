name: Aura Market Daily Update

on:
  schedule:
    # Runs at 15:08 UTC, which is 8:38 PM IST
    - cron: '08 15 * * *'
  workflow_dispatch: # Allows manual running from the Actions tab

jobs:
  update-scores:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository (with write permission)
      uses: actions/checkout@v3
      # GITHUB_TOKEN को पास करें ताकि write permission मिल सके
      with:
        token: ${{ secrets.GITHUB_TOKEN }} 

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install google-generativeai python-dateutil pytz

    - name: Run update script
      # यह env: ब्लॉक GEMINI_API_KEY को python script के लिए उपलब्ध कराता है
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: python update.py

    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add data.json
        
        # Pull करना ज़रूरी है, ताकि remote changes मिल सकें और rejected एरर ठीक हो।
        git pull --rebase origin main 
        
        # केवल तभी कमिट करें जब data.json में बदलाव हो
        git diff --quiet && git diff --staged --quiet || git commit -m "📈 Aura Market Daily Update"
        
        # FINAL SOLUTION: Push करने के लिए टोकन का उपयोग URL में करें (सबसे मज़बूत तरीका)
        git push "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}" main

